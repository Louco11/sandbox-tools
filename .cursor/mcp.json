{
  "mcpServers": {
    "sandbox": {
      "command": "${workspaceFolder}/bin/sandbox-mcp",
      "args": [
        "platform"
      ]
    },
    "sandbox-team-load": {
      "command": "${workspaceFolder}/bin/sandbox-mcp",
      "args": [
        "tool",
        "team-load"
      ]
    }
  }
}
