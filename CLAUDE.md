@AGENTS.md

## Для Claude Code

- Правила песочницы — в `AGENTS.md` (подключён выше): это единый файл для всех агентов. Здесь только то, что
  касается Claude Code; правила сюда не дублируй.
- MCP-серверы проекта — `.mcp.json`: `sandbox` (build-time) и `sandbox-<тул>` (run-time). Генерирует `make agents`.
