# Образ джоб Gitea Actions: только Node — checkout, контракт допуска, типы.
# Docker CLI здесь намеренно нет: выкатывает деплоер (infra/deployer), а не CI.
FROM node:22-bookworm
ENV NODE_OPTIONS=--disable-warning=ExperimentalWarning
